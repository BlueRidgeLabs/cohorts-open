%tr
  %td= link_to research_session.datetime.strftime('%b %e, %Y at %l:%M %p'), admin_engagement_research_session_path(@engagement, research_session)
  %td= raw research_session.people.collect { |person| link_to person.full_name, admin_person_path(person) }.join(", ")