%h1 Listing comments
%table
  %thead
    %tr
      %th Content
      %th User
      %th Commentable type
      %th Commentable
      %th
      %th
      %th
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.content
        %td= comment.user_id
        %td= comment.commentable_type
        %td= comment.commentable_id
        %td= link_to 'Show', admin_comment_path(comment)
        %td= link_to 'Edit', edit_admin_comment_path(comment)
        %td= link_to 'Destroy', admin_comment_path(comment), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Comment', new_admin_comment_path
